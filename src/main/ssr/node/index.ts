/**
 * NodeJS-specific tooling for SSR implementation that supports router hydration on the client.
 *
 * ```ts
 * import { NodeSSRRouter } from 'react-corsair/ssr/node';
 * ```
 *
 * @module ssr/node
 */

export { NodeSSRRouter } from './NodeSSRRouter.js';
