import { RouteMatch } from './matchRoutes';
import { SlotContent, SlotContentComponents } from './Slot';

export function deriveSlotContent(
  prevContent: SlotContent | undefined,
  routeMatches: RouteMatch[],
  options: SlotContentComponents
): SlotContent {
  return null!;
}
